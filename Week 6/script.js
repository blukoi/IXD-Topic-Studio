function distortsvg() {
    const middleSvg = document.getElementById('scroll1');
    const viewportHeight = window.innerHeight;
    const maxScrollableDistance = document.documentElement.scrollHeight - viewportHeight;
    const scrollPosition = window.pageYOffset;
    const scrollPercentage = (scrollPosition / maxScrollableDistance) * 100;
    let svgHeight = (scrollPercentage / 100) * viewportHeight - 100;

    svgHeight = Math.max(svgHeight, 4);
  
    middleSvg.style.height = `${svgHeight}px`;
}

window.addEventListener('scroll', distortsvg);

function distortsvg() {
    const middleSvg = document.getElementById('scroll2');
    const viewportHeight = window.innerHeight;
    const maxScrollableDistance = document.documentElement.scrollHeight - viewportHeight;
    const scrollPosition = window.pageYOffset;
    const scrollPercentage = (scrollPosition / maxScrollableDistance) * 100;
    let svgHeight = (scrollPercentage / 100) * viewportHeight - 100;

    svgHeight = Math.max(svgHeight, 32);
  
    middleSvg.style.height = `${svgHeight}px`;
}

function distortsvg() {
    const middleSvg = document.getElementById('scroll3');
    const viewportHeight = window.innerHeight;
    const maxScrollableDistance = document.documentElement.scrollHeight - viewportHeight;
    const scrollPosition = window.pageYOffset;
    const scrollPercentage = (scrollPosition / maxScrollableDistance) * 100;
    let svgHeight = (scrollPercentage / 100) * viewportHeight - 100;

    svgHeight = Math.max(svgHeight, 44);
  
    middleSvg.style.height = `${svgHeight}px`;
}

function distortsvg() {
    const middleSvg = document.getElementById('scroll4');
    const viewportHeight = window.innerHeight;
    const maxScrollableDistance = document.documentElement.scrollHeight - viewportHeight;
    const scrollPosition = window.pageYOffset;
    const scrollPercentage = (scrollPosition / maxScrollableDistance) * 100;
    let svgHeight = (scrollPercentage / 100) * viewportHeight - 100;

    svgHeight = Math.max(svgHeight, 32);
  
    middleSvg.style.height = `${svgHeight}px`;
}

function distortsvg() {
    const middleSvg = document.getElementById('scroll5');
    const viewportHeight = window.innerHeight;
    const maxScrollableDistance = document.documentElement.scrollHeight - viewportHeight;
    const scrollPosition = window.pageYOffset;
    const scrollPercentage = (scrollPosition / maxScrollableDistance) * 100;
    let svgHeight = (scrollPercentage / 100) * viewportHeight - 100;

    svgHeight = Math.max(svgHeight, 32);
  
    middleSvg.style.height = `${svgHeight}px`;
}

function distortsvg() {
    const middleSvg = document.getElementById('scroll6');
    const viewportHeight = window.innerHeight;
    const maxScrollableDistance = document.documentElement.scrollHeight - viewportHeight;
    const scrollPosition = window.pageYOffset;
    const scrollPercentage = (scrollPosition / maxScrollableDistance) * 100;
    let svgHeight = (scrollPercentage / 100) * viewportHeight - 100;

    svgHeight = Math.max(svgHeight, 32);
  
    middleSvg.style.height = `${svgHeight}px`;
}

function distortsvg() {
    const middleSvg = document.getElementById('scroll7');
    const viewportHeight = window.innerHeight;
    const maxScrollableDistance = document.documentElement.scrollHeight - viewportHeight;
    const scrollPosition = window.pageYOffset;
    const scrollPercentage = (scrollPosition / maxScrollableDistance) * 100;
    let svgHeight = (scrollPercentage / 100) * viewportHeight - 100;

    svgHeight = Math.max(svgHeight, 32);
  
    middleSvg.style.height = `${svgHeight}px`;
}

function distortsvg() {
    const middleSvg = document.getElementById('scroll8');
    const viewportHeight = window.innerHeight;
    const maxScrollableDistance = document.documentElement.scrollHeight - viewportHeight;
    const scrollPosition = window.pageYOffset;
    const scrollPercentage = (scrollPosition / maxScrollableDistance) * 100;
    let svgHeight = viewportHeight - (scrollPercentage / 100) * viewportHeight;
  
    svgHeight = Math.max(svgHeight, 32);
  
    middleSvg.style.height = `${svgHeight}px`;
}

function distortsvg() {
    const middleSvg = document.getElementById('scroll9');
    const viewportHeight = window.innerHeight;
    const maxScrollableDistance = document.documentElement.scrollHeight - viewportHeight;
    const scrollPosition = window.pageYOffset;
    const scrollPercentage = (scrollPosition / maxScrollableDistance) * 100;
    let svgHeight = viewportHeight - (scrollPercentage / 100) * viewportHeight;
  
    svgHeight = Math.max(svgHeight, 32);
  
    middleSvg.style.height = `${svgHeight}px`;
}

function distortsvg() {
    const middleSvg = document.getElementById('scroll10');
    const viewportHeight = window.innerHeight;
    const maxScrollableDistance = document.documentElement.scrollHeight - viewportHeight;
    const scrollPosition = window.pageYOffset;
    const scrollPercentage = (scrollPosition / maxScrollableDistance) * 100;
    let svgHeight = viewportHeight - (scrollPercentage / 100) * viewportHeight;
  
    svgHeight = Math.max(svgHeight, 32);
  
    middleSvg.style.height = `${svgHeight}px`;
}